import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.foccobrasil.cobrancacobradores",
  appName: "Cobrança Cobradores",
  webDir: "dist",
};

export default config;
